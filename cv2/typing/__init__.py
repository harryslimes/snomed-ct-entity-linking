"""
Stub submodule for `cv2.typing` used by some OpenCV wheels.

The real `cv2.typing` provides typing helpers. For our purposes we only need
imports to succeed for text-only workflows.
"""

from __future__ import annotations

from typing import Any

MatLike = Any

